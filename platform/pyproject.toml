[project]
name = "meridian"
version = "0.1.0"
description = "Meridian platform — orbit, registry, observations, scheduling and the public API."
requires-python = ">=3.11,<3.12"
license = { text = "Apache-2.0" }

dependencies = [
    "alembic>=1.13",
    "fastapi>=0.115",
    # Declared although FastAPI already installs it, because we import it
    # directly: meridian.api.request_limits is pure ASGI middleware and takes
    # its Scope/Receive/Send types from starlette.types. A package we import is
    # a package we depend on, whoever else happens to pull it in.
    "starlette>=0.40",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.9",
    "psycopg[binary,pool]>=3.2",
    "sgp4>=2.23",
    "skyfield>=1.49",
    "prometheus-client>=0.21",
]

[project.optional-dependencies]
# Fitting only (D-155). `meridian model fit` needs a numerical stack; scoring a
# pass does not, and the jobs service that scores passes runs on the station's
# Pi. So the stack is an extra the platform image never installs, and only
# meridian.prediction.fit may import it (tests/unit/test_prediction_boundaries.py).
fit = [
    "numpy>=2.0",
    "scikit-learn>=1.5",
]

[project.scripts]
meridian = "meridian.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/meridian"]
